apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-flux-app.fullname" . }}
  labels:
    {{- include "nginx-flux-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-flux-app.selectorLabels" . | nindent 4 }}
